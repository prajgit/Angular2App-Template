namespace Org.App.Web
{
    using IocServiceStack;
    internal class IocServiceStackConfig
    {
        internal static void Configure()
        {
            /* Global IoC Container Configuration of IocServiceStack */
            //IocServicelet.Configure(config =>
            //{
            //    config.AddServices(service =>
            //    {
            //        /*Configure BusinessService assembly as main service entry point*/
            //        service.Assemblies = new[] { "" };

            //        service.StrictMode = true;
            //    });
            //});
        }
    }
}
